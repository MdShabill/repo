@model List<MaterialPurchaseVm>

<style>
    body {
        background: linear-gradient(135deg, #1E3A5F, #0F172A);
        min-height: 100vh;
    }

    .page-container {
        padding: 30px;
        margin-top: 60px;
    }

    .card-box {
        background: #ffffff;
        border-radius: 16px;
        padding: 25px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }

    .page-title {
        text-align: center;
        font-weight: 700;
        color: #1E293B;
        margin-bottom: 15px;
    }

    .top-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 15px;
    }

    .btn-orange {
        background-color: #F59E0B;
        color: white;
        font-weight: 600;
        border-radius: 10px;
        padding: 8px 18px;
        border: none;
        text-decoration: none;
    }

        .btn-orange:hover {
            background-color: #d97706;
            color: white;
        }

    .filter-row {
        display: flex;
        align-items: center;
        gap: 10px;
        flex-wrap: wrap;
        margin-bottom: 15px;
    }

    .form-control, .form-select {
        border-radius: 8px;
        padding: 6px 10px;
        font-size: 14px;
        width: 150px;
    }

        .form-control:focus, .form-select:focus {
            border-color: #F59E0B;
            box-shadow: 0 0 5px rgba(245,158,11,0.5);
        }

    .btn-go {
        background-color: #F59E0B;
        color: white;
        border-radius: 8px;
        padding: 6px 15px;
        border: none;
    }

        .btn-go:hover {
            background-color: #d97706;
        }

    .table {
        display: block;
        overflow-x: auto;
        white-space: nowrap;
    }

        .table thead {
            background-color: #1E3A5F;
            color: white;
        }

        .table tbody tr:nth-child(even) {
            background-color: #f8fafc;
        }

        .table tbody tr:hover {
            background-color: #eef2f7;
        }

    .no-record {
        text-align: center;
        padding: 30px;
        font-weight: 600;
        color: #6c757d;
    }
</style>

<div class="container page-container">

    <div class="card-box">

        <!-- Top -->
        <div class="top-bar">
            <div></div>

            <h3 class="page-title">Material Purchase Report</h3>

            <a asp-controller="MaterialPurchase" asp-action="Add" class="btn-orange">
                + Add New
            </a>
        </div>

        <!-- Messages -->
        <div class="text-center">
            <span class="text-success">
                <b>@TempData["SuccessMessage"] @TempData["DeleteSuccessMessage"]</b>
            </span>
        </div>

        <!-- Total -->
        <div class="mb-2">
            <b>Total Purchase: @ViewBag.materialPurchaseCount</b>
        </div>

        <form method="get" asp-action="Index" onsubmit="return validateDates()" class="filter-row">

            <label><b>From:</b></label>
            <input type="date" id="DateFrom" name="DateFrom" value="@ViewBag.DateFrom" class="form-control" />

            <label><b>To:</b></label>
            <input type="date" id="DateTo" name="DateTo" value="@ViewBag.DateTo" class="form-control" />

            <label><b>Supplier:</b></label>
            @Html.DropDownList("SupplierId", (SelectList)ViewBag.Supplier, "-- Select --", new { @class = "form-select" })

            <label><b>Brand:</b></label>
            @Html.DropDownList("BrandId", (SelectList)ViewBag.Brands, "-- Select --", new { @class = "form-select" })

            <label><b>Material:</b></label>
            @Html.DropDownList("MaterialId", (SelectList)ViewBag.Materials, "-- Select --", new { @class = "form-select" })

            <button type="submit" class="btn-go">Go</button>

        </form>

        <span id="errorMessage" class="text-danger"></span>

        <!-- Table -->
        @if (Model != null && Model.Count > 0)
        {
            <table class="table table-hover text-center align-middle">
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Supplier</th>
                        <th>Supplier Contact</th>
                        <th>Brand</th>
                        <th>Material</th>
                        <th>Unit Price</th>
                        <th>Quantity</th>
                        <th>Material Cost</th>
                        <th>Delivery Charge</th>
                        <th>Total Amount</th>
                        <th>Action</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        var totalAmount = item.MaterialCost + item.DeliveryCharge;

                        <tr>
                            <td>@item.Date.ToString("dd-MMM-yyyy")</td>
                            <td>@item.SupplierName</td>
                            <td>@item.PhoneNumber</td>
                            <td>@item.BrandName</td>
                            <td>@item.MaterialName</td>
                            <td>@item.UnitPrice.ToString("0.00") / <i>@item.UnitOfMeasure</i></td>
                            <td>@item.Quantity.ToString("0.00") / <i>@item.UnitOfMeasure</i></td>
                            <td>@item.MaterialCost</td>
                            <td>@item.DeliveryCharge</td>
                            <td><b>₹ @totalAmount</b></td>
                            <td>
                                <form asp-action="Delete" method="post" style="display:inline;"
                                      onsubmit="return confirm('Are you sure you want to delete this record?');">
                                    <input type="hidden" name="Id" value="@item.Id" />
                                    <button type="submit" class="btn btn-link text-danger p-0">Delete</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="no-record">
                🚫 No Record Found
            </div>
        }

    </div>
</div>

<script>
    function validateDates() {
        var dateFrom = document.getElementById("DateFrom").value;
        var dateTo = document.getElementById("DateTo").value;
        var errorMessage = document.getElementById("errorMessage");
        errorMessage.innerHTML = "";

        var currentDate = new Date().toISOString().split('T')[0];
        var fromDate = dateFrom ? new Date(dateFrom) : null;
        var toDate = dateTo ? new Date(dateTo) : null;

        if (fromDate && toDate && fromDate > toDate) {
            errorMessage.innerHTML = "FROM DATE cannot be greater than TO DATE.";
            return false;
        }

        if (dateTo && dateFrom > currentDate || dateFrom && dateTo > currentDate) {
            errorMessage.innerHTML = "FROM DATE and TO DATE cannot be in the future.";
            return false;
        }
        return true;
    }
</script>